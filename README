UFile Java SDK

1. 目录说明

./v1/UFileSDK/src
    SDK的源码目录，可将此目录打包成jar用于其他项目。
UFileSDK依赖的jar都在./v1/UFileSDK/libs目录下：
    ./v1/UFileSDK/libs/okhttp-3.9.0.jar
    ./v1/UFileSDK/libs/okio-1.13.0.jar
    ./v1/UFileSDK/libs/gson-2.8.2.jar

./v1/UFileSDK/test
    UFileSDK的使用Demo

2. 配置文件
配置文件示例
./v1/UFileSDK/src/config.properties

配置文件中有5个参数
Bucket=paste your bucket here
UCloudPublicKey=paste your public key here
UCloudPrivateKey=paste your private key here
ProxySuffix=paste your proxy suffix here if any
GlobalUploadProxySuffix=paste your global upload proxy suffix here if any
GlobalDownloadProxySuffix=paste your global download proxy suffix here if any
CDNHost=paste your cdn speed up host here if any

说明：
Bucket 请改成用户的bucket
UCloudPublicKey 请改成用户的公钥
UCloudPrivateKey 请改成用户的私钥
ProxySuffix 单地域指定上传域名的后缀，可以填写源站的后缀(例如北京地域 .cn-bj.ufileos.com)或内网域名的后缀
GlobalUploadProxySuffix 全球化上传域名后缀
GlobalDownloadProxySuffix 全球化下载域名后缀
CDNHost CDN加速域名，例如 speed-ufile.ucloud.com.cn

发送请求前，须加载配置项或者逐个设置配置项，在同时设置的情况下，默认使用优先级: CDN加速 > 全球化 > 单地域，可自行修改
